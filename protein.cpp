#include "Protein.h"
Protein::~Protein() = default;

bool Protein::isEnough(double protein) {
    //Says if the food has enough protein to mantain health
    return false; // 
}

bool Protein::isLean(double fats, double protein) {
    //Says if the food has enough protein to mantain health
    return false; // 
}

std::vector<std::string> Protein::show(const std::string& proteinType) {
    // Shows the nutritional values of said protein
    return {}; // Placeholder return
}

