#include "Carbohydrates.h"

Carbohydrates::~Carbohydrates() = default;

bool Carbohydrates::isHealthy(double sugar, double fiber) {
    // Measures if the ammount of sugar is appropriate
    return false; // If it has too much sugar it says false
}

std::vector<std::string> Carbohydrates::show(const std::string& options) {
    // Displays a list of all the foods that the user has entered which are carbs
    return {}; // Returns a vector of all the possible options for carbohydrates
}